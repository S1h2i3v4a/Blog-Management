const sessionSecret = "your_secret_key";

// JWT settings
const jwtSecret = "your_jwt_secret_key";
const jwtExpiresIn = "2h";

// Reset password token expiry (ms)
const resetPasswordTokenExpiryMs = 1000 * 60 * 60; // 1 hour

const emailUser = "shivamkesharishivkeshari990@gmail.com";
const Password = "ftuatuhkqrzmxfyl";

module.exports = {
  sessionSecret,
  jwtSecret,
  jwtExpiresIn,
  resetPasswordTokenExpiryMs,
  emailUser,
  Password,
};
